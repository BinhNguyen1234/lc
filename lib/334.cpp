#include "../include/334.h"


bool increasingTriplet(vector<int>& nums){
  int min1 = nums[0];
  int min2 = nums[0];
  for(int i = 1; i < nums.size(); i++){
    if(nums[i] <  min1){
      min1 = nums[i];
    }
    if(nums[i] > min1] && nums[i] < mins)
  }

  return false;
}

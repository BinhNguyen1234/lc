#ifndef Binary
#define Binary
#include <vector>
#include <queue>
using namespace std;

int convertToDecimal(vector<int> bNumber);
vector<int> convertToBinary(int dNumber);

int convertToDecimal(queue<int> bNUmber, int backtracking);
#endif
